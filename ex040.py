#40
data = "   삼성전자   " #data에 대해서 strip함수를 적용시켰다.
a = data.strip() # strip()은 특정문자를 제거할수있다.
#strip()에 공백을 두면서 좌우의 공백을 제거했다. 
#a = data.strip()을 하면서 a에 strip된 값을 대입했다.
print(a)
print(data)# print(data.strip())과 비교해보기위해서 사용하였다.